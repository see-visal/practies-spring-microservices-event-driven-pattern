services:
  oracle19-itp:
    image: container-registry.oracle.com/database/enterprise:19.3.0.0
    container_name: oracle19-itp
    ports:
      - "1521:1521"
    environment:
      ORACLE_PWD: Oracle@168
      ENABLE_ARCHIVELOG: true
    volumes:
      - oracle_data_itp:/opt/oracle/oradata
      - oracle_fra_itp:/opt/oracle/fast_recovery_area
    shm_size: '1g'
    networks:
      - itp-net

volumes:
  oracle_data_itp:
  oracle_fra_itp:

networks:
  itp-net:
    external: true
